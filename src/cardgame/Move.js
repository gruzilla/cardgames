define('cardgame/Move', function() {
    'use strict';

    // the constructor
    return function(source, target, options) {
        console.debug('create move...');
        //console.debug(source, target, options);
    };
});